name: Linux Kernel Compiler

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses-dev bison flex libssl-dev libelf-dev bc ccache devscripts debhelper rsync

    - name: Download and Extract Kernel Source
      run: |
        wget https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.12.86.tar.xz
        tar -xf linux-6.12.86.tar.xz
        # استخدام المسار الكامل للمستودع لضمان نسخ الملف المخفي .config بنجاح
        cp "$GITHUB_WORKSPACE/.config" linux-6.12.86/.config

    - name: Prepare and Fix Configuration
      run: |
        cd linux-6.12.86
        # تعطيل مفاتيح التوقيع الرقمي لمنع توقف البناء
        scripts/config --disable SYSTEM_TRUSTED_KEYS
        scripts/config --disable SYSTEM_REVOCATION_KEYS
        # تطبيق الإعدادات واعتماد الخيارات الافتراضية للميزات الجديدة
        make olddefconfig

    - name: Compile Kernel (Turbo Mode)
      run: |
        cd linux-6.12.86
        # بناء حزم الديبان باستخدام كافة أنوية المعالج المتاحة
        make -j$(nproc) bindeb-pkg

    - name: Organize Artifacts
      run: |
        mkdir -p output
        # نقل كافة حزم .deb الناتجة بنجاح إلى مجلد المخرجات
        mv ../*.deb output/ 2>/dev/null || mv linux-6.12.86/../*.deb output/ || true

    - name: Upload Compiled Kernel
      uses: actions/upload-artifact@v4
      with:
        name: custom-debian-kernel
        path: output/
